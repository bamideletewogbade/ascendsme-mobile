import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App builds without errors', (WidgetTester tester) async {
    // Placeholder test — full integration tests are in test/integration/
    expect(true, isTrue);
  });
}
